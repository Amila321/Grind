spring.application.name=grind-backend

server.port=${PORT:8080}

spring.profiles.default=local

spring.jpa.open-in-view=false

app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}